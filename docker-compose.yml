version: "3.9"

services:
  db:
    image: postgres:15
    container_name: myapp_db
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-myapp}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d myapp"]
      interval: 5s
      timeout: 5s
      retries: 5

  migration:
    build: 
      context: ./backend/MyApp.Api
    container_name: myapp_migration
    # Runs the bundle created in the Dockerfile
    entrypoint: ./migrate
    command: --connection "Host=db;Port=5432;Database=myapp;Username=postgres;Password=postgres"
    depends_on:
      db:
        condition: service_healthy

  backend:
    build: 
      context: ./backend/MyApp.Api
    container_name: myapp_backend
    ports:
      - "5105:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=db;Port=5432;Database=myapp;Username=postgres;Password=postgres
      - Jwt__Secret=${JWT_SECRET:-your-super-secret-key-at-least-32-characters-long!!}
      - Jwt__Issuer=${JWT_ISSUER:-MyApp}
      - Jwt__Audience=${JWT_AUDIENCE:-MyApp}
      - Jwt__AccessTokenMinutes=${JWT_ACCESS_TOKEN_MINUTES:-15}
      - Jwt__RefreshTokenDays=${JWT_REFRESH_TOKEN_DAYS:-7}
    depends_on:
      db:
        condition: service_healthy
      migration:
        condition: service_completed_successfully

  frontend:
    build: ./frontend
    container_name: myapp_frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  postgres_data: